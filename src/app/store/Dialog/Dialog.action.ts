import { createAction, props } from "@ngrx/store";

export const usedConditionDialog = createAction("[body] Afficher les conditions d'utilisation");
export const aboutDialog = createAction("[body] Afficher les informations à propos");